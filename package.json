{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:frontend": "yarn workspace @draw-and-guess/frontend build",
    "start:server": "yarn workspace @draw-and-guess/server start:production"
  }
}
